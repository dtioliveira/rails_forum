<section>
  <div class="w-container main">
    <h1 class="page-header">Área do admin</h1>
    <div class="w-row">
      <div class="w-col w-col-3 w-clearfix">
        <%= render 'shared/user_card' %>
      </div>

      <div class="w-col w-col-9">
        <div class="pull-right" style="margin-bottom: 20px;">
          <%= link_to "Novo vídeo", new_video_path, class: 'w-button' %>
        </div>
        <% if @videos.any? %>
          <%= render 'videos_list' %>
        <% else %>
          <div class="alert alert-info">
            <strong>Videos: </strong>Não há videos no momento.</div>
        <% end %>
      </div>
    </div>
  </div>
</section>
